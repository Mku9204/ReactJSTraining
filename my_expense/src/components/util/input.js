import React from "react";
import { TextField } from "@mui/material";
const Input = (props) => {
    return (
        // <input {...props} />
        <TextField {...props} />
    )
}
export default Input;