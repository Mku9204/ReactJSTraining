import { makeStyles } from "@mui/styles";

const inputStyle = makeStyles({
    nText1: {
        marginTop: '10px',
        borderRadius: '30px',
        boxShadow: '5px 5px rgb(110 108 108)',
        height: '20px',
    },
});
export default inputStyle;